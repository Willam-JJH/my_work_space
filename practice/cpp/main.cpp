#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef double dou;
typedef string str;
typedef pair<ll, ll> pll;
#define lowbit(x) (x & -x)

const int N = 1e2 + 10;

vector<pll> v;
ll s[N];
ll l[N];

void init()
{
}

void solve()
{
    ll n, m;
    scanf("%lld %lld", &n, &m);
}

int main()
{
    // freopen("speeding.in", "r", stdin);
    // freopen("speeding.out", "w", stdout);
    ll t = 1;
    // cin >> t;
    init();
    while (t--)
        solve();
    return 0;
}